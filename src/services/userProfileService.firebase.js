/**
 * User Profile Service — Firebase Stub
 * ======================================
 * Stub implementation for Firebase backend.
 * The active backend is Supabase; this file exists only to satisfy
 * the static import in userProfileService.js and prevent build failures.
 */

export async function ensureUserProfile() {
    console.warn('[userProfileService.firebase] Firebase backend not implemented — using Supabase');
    return null;
}

export async function updateUserProfile() {
    console.warn('[userProfileService.firebase] Firebase backend not implemented — using Supabase');
}
